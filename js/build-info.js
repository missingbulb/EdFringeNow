/* Overwritten at deploy time by the GitHub Pages workflow with the deployed
 * commit's short hash and time. The committed values below are the local-dev
 * fallback (shows "dev" on the debug chip when not built by the workflow). */
window.__BUILD__ = { hash: "dev", time: "" };
