# THROWAWAY PROBE — deleted before the PR (see .claude/skills/probe-edfringe-api).
# Runs scraper/probe_nominatim.sh on an open-network runner; path-filtered to
# that script so nothing else can fire it.
name: Probe Nominatim (throwaway)

on:
  push:
    branches:
      - claude/now-page-non-show-locations-i51l07
    paths:
      - scraper/probe_nominatim.sh

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Probe
        run: bash scraper/probe_nominatim.sh
