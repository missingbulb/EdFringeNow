"use strict";

module.exports = {
  description: "first visit shows the explainer: kicker, title, three steps, Got it button",
  // No storage seed: a first-time visitor has no dismissal remembered.
};
