// Kind: behavior — a driven gesture and its consequence, asserted in code
// against the harness's fakes (a URL built, a class toggled, storage written,
// a download's bytes) — what a still image genuinely cannot see. No PNG may
// live in this folder (the coverage gate enforces it).
// Runner: behavior.test.js (the `test:ui` lane).
"use strict";

module.exports = { image: false };
